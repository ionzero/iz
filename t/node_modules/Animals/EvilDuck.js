var iz = require('iz');
//iz.use('Animals.Duck');

module.exports = iz.Package('Animals.EvilDuck', { extends: 'Animals.Duck' }, function (Class) {
   
   Class.has( 'quack', { default: 'Evil quack'}); 
   
   return Class;
});